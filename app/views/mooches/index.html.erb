<h1>Listing mooches</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @mooches.each do |mooch| %>
      <tr>
        <td><%= mooch.description %></td>
        <td><%= mooch.user %></td>
        <td><%= link_to 'Show', mooch %></td>
        <td><%= link_to 'Edit', edit_mooch_path(mooch) %></td>
        <td><%= link_to 'Destroy', mooch, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mooch', new_mooch_path %>
