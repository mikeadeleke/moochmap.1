<ul>
  <% @mooches.each do |mooch| %>
    <li><%= mooch.description %></li>
    <li><%= time_ago_in_words(l mooch.created_at) %> ago</li>
  <% end %>
</ul>


<%= link_to t('.new', :default => t("helpers.links.new")),
            new_mooch_path,
            :class => 'btn btn-primary' %>
