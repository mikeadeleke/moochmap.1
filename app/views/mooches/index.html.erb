<% @mooches.each do |mooch| %>
  <%= mooch.description %>
  <%=l mooch.created_at, format: :long %>
<% end %>


<%= link_to t('.new', :default => t("helpers.links.new")),
            new_mooch_path,
            :class => 'btn btn-primary' %>
